// import { fromJS } from 'immutable';
// import { selectSubmitPageGeneratorDomain } from '../selectors';

describe('selectSubmitPageGeneratorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
